version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.10"
  commands:
    # ensure the RTD output folder exists
    - mkdir -p $READTHEDOCS_OUTPUT/html/
    # copy your pre-built docs into that folder
    - cp --recursive docs/build/html/* $READTHEDOCS_OUTPUT/html/